import React from 'react';
import loader from '../assets/images/load.svg'

const Loader = () => {
    return (
        <div>
            <img src={loader} alt=""/>
        </div>
    );
};

export default Loader;